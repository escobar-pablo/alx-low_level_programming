#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Print alphabets in reverse'
 * Return: 0 (Success)
 */
int main(void)
{
	int n;
	int m;

	for (n = 97; n <= 57; n++)
	{
		putchar(n);
	}
	for (m = 97; m <= 102; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
